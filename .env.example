# ═══════════════════════════════════════════════════════════════════════════════
# SANGIA ECOSYSTEM — UNIFIED .env TEMPLATE
# Gunakan sebagai referensi untuk SEMUA aplikasi: sangia-apis, sangia-sikola,
# sdg-mapper, sdgs-analytics, sdg-mono. Setiap app hanya aktifkan setting yang relevan.
# ═══════════════════════════════════════════════════════════════════════════════

# ── SECTION 1: Aplikasi (sangia-apis only) ────────────────────────────────────────
# Hanya digunakan oleh sangia-apis
APP_ENV=development
APP_DEBUG=false
APP_URL=https://api.sangia.org

# ── SECTION 2: Database (SHARED — identik di SEMUA aplikasi) ──────────────────────
# Wajib di: sangia-apis, sangia-sikola, sdg-mapper, sdgs-analytics (opsional), sdg-mono
# Driver: mysql | mariadb | pgsql
DB_DRIVER=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=sangia_ecosystem
DB_USERNAME=root
DB_PASSWORD=your_secure_password
DB_CHARSET=utf8mb4

# ── SECTION 3: API Key Authentication (SHARED — IDENTICAL di SEMUA aplikasi) ──────
# WAJIB SAMA di: sangia-apis, sangia-sikola, sdg-mapper, sdgs-analytics, sdg-mono
# Setiap app bisa generate keys; sangia-apis memvalidasi dengan secret yang sama.
# Generate once: openssl rand -hex 32
SANGIA_SHARED_SECRET=generate_64_character_random_hex_string_here
# Comma-separated user_id list that can call /api/v1/admin/keys/revoke
SANGIA_ADMIN_USER_IDS=service-api,superadmin
# Query-string API keys are disabled by default to avoid leaking credentials in URL logs.
SANGIA_ALLOW_QUERY_API_KEY=false

# ── SECTION 4: Rate Limiting (sangia-apis only) ────────────────────────────────────
# Hanya digunakan oleh sangia-apis
RATE_LIMIT_REQUESTS=60
RATE_LIMIT_WINDOW=60

# ── SECTION 5: CORS (sangia-apis only) ─────────────────────────────────────────────
# Hanya digunakan oleh sangia-apis untuk validasi origin frontend
CORS_ALLOWED_ORIGINS=http://localhost:3000,https://www.sangia.org

# ── SECTION 6: JWT (sangia-apis only) ──────────────────────────────────────────────
# Hanya digunakan oleh sangia-apis jika menggunakan JWT auth
JWT_SECRET=generate_64_character_random_string_here_and_keep_it_secret
JWT_TTL=86400

# ── SECTION 7: External APIs (SHARED — di apps yang membutuhkan) ───────────────────

# ORCID OAuth2 (digunakan oleh: sangia-apis, sangia-sikola, sdg-mapper)
ORCID_CLIENT_ID=
ORCID_CLIENT_SECRET=
ORCID_REDIRECT_URI=https://www.sangia.org/auth/orcid-callback
ORCID_SANDBOX=true

# Scopus API (digunakan oleh: sangia-apis, sangia-sikola, sdg-mapper)
SCOPUS_API_KEY=

# Semantic Scholar API (digunakan oleh: sangia-apis)
# Official API key untuk higher rate limits. Get at: https://www.semanticscholar.org/product/api
SEMANTIC_SCHOLAR_API_KEY=

# PubMed / NCBI E-utilities (digunakan oleh: sangia-apis)
# API key raises rate limit dari 3 req/s ke 10 req/s. Get at: https://www.ncbi.nlm.nih.gov/account/
PUBMED_API_KEY=

# OpenAlex API (digunakan oleh: sangia-apis)
# No API key required. Email used untuk polite pool (faster responses).
OPENALEX_MAILTO=api@sangia.org

# Crossref API (digunakan oleh: sangia-apis)
# No API key required untuk basic access
CROSSREF_MAILTO=api@sangia.org

# ── SECTION 8: Frontend URLs (SHARED di frontend apps) ────────────────────────────

# Digunakan oleh: sdg-mapper, sdgs-analytics, sangia-sikola (untuk redirect/links)
FRONTEND_URL=https://www.sangia.org
REACT_APP_API_URL=https://api.sangia.org
REACT_APP_API_KEY=sg_your_client_id_here

# ── SECTION 9: Internal APIs (Aplikasi saling memanggil) ──────────────────────────

# Digunakan oleh: sangia-sikola, sdg-mapper, sdgs-analytics untuk call sangia-apis
SANGIA_API_URL=https://api.sangia.org
SANGIA_API_KEY=sg_app_id_generated_in_sangia_sikola

# ═══════════════════════════════════════════════════════════════════════════════
# NOTES UNTUK SETIAP APLIKASI:
# ═══════════════════════════════════════════════════════════════════════════════
# sangia-apis:
#   ✓ SECTION 1 (APP_*), SECTION 2 (DB_*), SECTION 3 (SANGIA_SHARED_SECRET),
#     SECTION 4 (RATE_LIMIT_*), SECTION 5 (CORS_*), SECTION 6 (JWT_*),
#     SECTION 7 (semua external API keys), SECTION 8 (FRONTEND_URL)
#
# sangia-sikola (OJS):
#   ✓ SECTION 2 (DB_*), SECTION 3 (SANGIA_SHARED_SECRET),
#     SECTION 7 (ORCID_*, SCOPUS_API_KEY, OPENALEX_MAILTO),
#     SECTION 8 (FRONTEND_URL, REACT_APP_*), SECTION 9 (SANGIA_API_*)
#
# sdg-mapper (React + PHP):
#   ✓ SECTION 2 (DB_*), SECTION 3 (SANGIA_SHARED_SECRET),
#     SECTION 7 (ORCID_*, SCOPUS_API_KEY, OPENALEX_MAILTO),
#     SECTION 8 (FRONTEND_URL, REACT_APP_*), SECTION 9 (SANGIA_API_*)
#
# sdgs-analytics (React Dashboard):
#   ✓ SECTION 2 (DB_*), SECTION 3 (SANGIA_SHARED_SECRET),
#     SECTION 8 (FRONTEND_URL, REACT_APP_*), SECTION 9 (SANGIA_API_*)
#
# sdg-mono (Legacy PHP):
#   ✓ SECTION 2 (DB_*), SECTION 3 (SANGIA_SHARED_SECRET),
#     SECTION 9 (SANGIA_API_*)
# ══════════════════════════════════════════════════════════════════════════════

